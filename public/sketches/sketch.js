const D  = 400;
const CS = 20;
const C  = [D/2,D/2,CS];
let PO = [C[0], C[1]];

function setup() {
  createCanvas(D,D);
  background(1);
  console.log('hlo from setup',{canvas: {D}});
}

function draw() {

}
