/* eslint-disable no-undef */
const D  = 400;

// eslint-disable-next-line no-unused-vars
function setup() {
    createCanvas(D,D);
    background(1);
    console.log('hlo from setup', {canvas: {D}});
}

// eslint-disable-next-line no-unused-vars
function draw() {

}
